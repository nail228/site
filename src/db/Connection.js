













/* const {Pool}=require('pg')

const clientDb=new Pool({
    host                 : ' db-postgresql-ams3-09748-do-user-12797020-0.b.db.ondigitalocean.com',            // Postgres ip address[s] or domain name[s]
    port                 : 25060,
    database             : 'defaultdb',
    username             : 'doadmin',
    password             : 'AVNS_xni9wd_X_wwHoc57Wss',
    ssl              :'required'
})
clientDb
    .connect()
    .then(() => console.log('connected'))
    .catch((err) => console.error('connection error', err.stack))
 */








//import postgres from "postgres"

//const sql = postgres( 'postgres://username:password@host:port/database',{
  //  host                 : ' db-postgresql-ams3-09748-do-user-12797020-0.b.db.ondigitalocean.com',            // Postgres ip address[s] or domain name[s]
  //  port                 : 25060,
   // database             : 'defaultdb',
   // username             : 'doadmin',
   // password             : 'AVNS_xni9wd_X_wwHoc57Wss',
   // ssl              :'required'
//})
//export default sql
